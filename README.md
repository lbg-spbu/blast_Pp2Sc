# TODO ...

## Install libraries

```shell
python -m venv venv
```

```shell
source venv/bin/activate
# If conda is available:
conda deactivate
```

```shell
pip install -r requirements.txt
```

## Using git

- `git branch`
- `git checkout -b <name>`
- `git add -A`
- `git commit -m "message"`
- `git push`

## ...

makeblastdb -in protein_sequence.fasta -parse_seqids -dbtype prot -out txid559292


